// import fastify from "../server"
//
// fastify.ready(err => {
//     if (err) throw err;
//     fastify.io.on("connection", (socket) => {
//         console.log(`Client connected: ${socket.id}`);
//     })
// })